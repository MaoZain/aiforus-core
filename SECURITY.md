# Security Policy

Do not report credentials, unpublished course content, or exploitable security
details in a public issue. Use GitHub's private security advisory flow for this
repository.

Published packages never require consumer credentials. Renderer packages must
not access Node.js, Electron IPC, the filesystem, cookies, authentication
storage, or host application secrets unless an explicitly versioned and
reviewed application adapter is introduced.
