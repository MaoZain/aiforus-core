# Changesets

Every user-visible package change must include a Changeset. Run `pnpm changeset`,
select the affected packages, choose the SemVer impact, and describe the public
change. Do not add a Changeset for documentation-only repository changes that do
not alter a published package.
